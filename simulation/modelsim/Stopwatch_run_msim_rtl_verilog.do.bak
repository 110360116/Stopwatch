transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+D:/0604/Stopwatch {D:/0604/Stopwatch/Stopwatch.v}
vlog -vlog01compat -work work +incdir+D:/0604/Stopwatch {D:/0604/Stopwatch/LCD_Controller.v}

